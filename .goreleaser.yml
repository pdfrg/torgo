project_name: tqbtui

before:
  hooks:
    - go mod tidy

builds:
  - id: tqbtui
    main: .
    binary: tqbtui
    ldflags:
      - -s -w
    goos:
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - id: tqbtui
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else }}{{ .Arch }}{{ end }}
    format: tar.gz

release:
  draft: true
